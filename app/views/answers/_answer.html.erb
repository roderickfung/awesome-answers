<div id = '<%=dom_id(answer)%>'>
  <%=link_to 'Delete',question_answer_path(@question, answer),
                      method: :delete,
                      remote: true, data: {confirm: 'Are you sure?'}%> | <%=answer.body%>
</div>
